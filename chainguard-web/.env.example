# ChainGuard 前端环境变量示例（复制为 .env.development / .env.local 使用）

# 数据源模式：api = 接真实后端（默认）；mock = 走内置演示数据（workflowStore）
DATA_MODE=api

# 后端地址由 config/config.ts 的 proxy 决定（/api -> http://127.0.0.1:8000），如需改动在 proxy 配置。
