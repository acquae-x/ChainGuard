pytest
# 0.16 起默认规则集发生变化；固定到导师评审版实际验证过的版本，避免 CI 漂移。
ruff==0.15.21
# 演示数据生成脚本（scripts/generate_enterprise_demo_data.py）写 xlsx 账套与 PDF 单据所需
openpyxl>=3.1
reportlab>=4.0
# P0-3：buyer/boss PDF 脱敏对照测试用 pypdf 做真实文本提取
pypdf>=4.0
# tools/build_tech_doc_pdf.py：把 docs/*.md 渲染成 PDF（另需 node 与本机 Chrome/Edge）
markdown>=3.5
pip-audit>=2.9
